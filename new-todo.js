"use strict";

window.onload = function () {
    fetchUserNames();
    fetchCategoryNames();

//Assuming you have a function to fetch user names and category names from the API
    function fetchUserNames() {
        // Call API to get user names and populate the #userName dropdown
    }

    function fetchCategoryNames() {
        // Call API to get category names and populate the #category dropdown
    }

    function submitToDo() {
        // Implement the logic to gather form data and submit ToDo task
        // You can use document.getElementById('elementId').value to get the values
        // Perform any additional validations or API calls if necessary
    }
}